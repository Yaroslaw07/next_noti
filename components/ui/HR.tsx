import { Box } from "@mui/material";

const HR = () => {
  return (
    <Box
      sx={{
        width: "50%",
        minHeight: "2px",
        backgroundColor: "primary.main",
        margin: "0 auto",
      }}
    />
  );
};

export default HR;
