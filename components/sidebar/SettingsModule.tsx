import { Icons } from "@/components/Icons";
import SidebarWrapper from "./SidebarItem";

const SettingsModule = () => {
  return <SidebarWrapper Icon={Icons.Settings} title={"Settings"} />;
};

export default SettingsModule;
